sudo: false
language: node_js
node_js:
  - '5.1.1'

install:
  - npm install -g npm
  - npm install -g bower
  - npm install -g phantomjs
  - npm install
  - bower install
  - echo "$(phantomjs -v)"

script:
  - ./node_modules/.bin/ember try:testall
